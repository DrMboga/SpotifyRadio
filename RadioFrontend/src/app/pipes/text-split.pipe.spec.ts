import { TextSplitPipe } from './text-split.pipe';

describe('TextSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new TextSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
